<canvas
	class="bicycle-canvas"
	data-bicycle-canvas
	aria-hidden="true"
></canvas>

<style is:global>
	.bicycle-canvas {
		position: fixed;
		inset: 0;
		width: 100%;
		height: 100%;
		z-index: 0;
		pointer-events: none;
		opacity: 0.42;
	}

	.bicycle-park-hit {
		position: fixed;
		width: 6.5rem;
		height: 4.25rem;
		z-index: 4;
		padding: 0;
		border: none;
		background: transparent;
		cursor: none;
		pointer-events: none;
		appearance: none;
	}

	.bicycle-park-hit.is-on {
		pointer-events: auto;
	}

	body > header,
	body > main,
	body > footer {
		position: relative;
		z-index: 1;
	}

	@media (prefers-reduced-motion: reduce) {
		.bicycle-canvas,
		.bicycle-park-hit {
			display: none;
		}
	}
</style>

<script>
	const canvas = document.querySelector<HTMLCanvasElement>('[data-bicycle-canvas]');
	if (canvas && !(canvas as HTMLCanvasElement & { __bikeMounted?: boolean }).__bikeMounted) {
		(canvas as HTMLCanvasElement & { __bikeMounted?: boolean }).__bikeMounted = true;
		const reduceMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
		if (!reduceMotion) {
			void import('../lib/bicycle/scene').then(({ mountBicycleScene }) => {
				mountBicycleScene(canvas);
			});
		}
	}
</script>
